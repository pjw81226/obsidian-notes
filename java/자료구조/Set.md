> 순서가 없고 데이터 중복 허용을 안한다.

**HashSet**
- 가장 빠르고 일반적인 Set
- 해싱(Hashing)을 이용해 구현한다.
- 중복을 제거하고 순서가 중요하지 않을 때 사용한다.

**TreeSet**
- 데이터를 저장할 때 자동으로 **정렬** 된다. (이진 탐색 트리 구조)
- 중복을 제거하면서 데이터를 정렬된 상태로 유지해야 할 때

**LinkedHashSet**
- 데이터가 입력된 **순서**를 기억한다.
- 중복은 제거하되 들어온 순서는 유지하고 싶을 때